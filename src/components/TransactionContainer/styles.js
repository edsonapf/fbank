import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  titleSection: {
    flexDirection: 'row',
    marginTop: 8,
  },
  childrenContainer: {
    flex: 1,
    flexGrow: 1,
  },
});

export default styles;
